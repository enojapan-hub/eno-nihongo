import { supabase } from "@/integrations/supabase/client";

export type AdaptiveTaskType = "new_kanji" | "new_vocabulary" | "new_grammar" | "review" | "quiz" | "reading" | "listening";
export type AdaptiveSuggestion = { id: string; label: string; subtitle?: string | null };
export type AdaptiveTask = { id:string; task_type:AdaptiveTaskType; target_count:number; completed_count:number; priority:number; reason:string|null; metadata:Record<string,unknown>|null; suggestions?:AdaptiveSuggestion[] };
export type AdaptivePlan = { active:boolean; targetLevel:string|null; targetDate:string|null; daysLeft:number|null; tasks:AdaptiveTask[]; target:number; completed:number };
const emptyPlan:AdaptivePlan={active:false,targetLevel:null,targetDate:null,daysLeft:null,tasks:[],target:0,completed:0};
type ProgressRow={item_type:string;item_id:string;status:string;due_at:string|null;ease_factor?:number|null};
type ReviewRow={item_type:string;item_id:string;rating:number;used_hint:boolean|null;response_ms:number|null;direction:string|null;aspect:string|null;created_at:string|null};
const aspectLabel:Record<string,string>={meaning:"Arti",reading:"Bacaan",usage:"Penggunaan",function:"Fungsi",context:"Konteks",confusion:"Membedakan materi mirip",meaning_reading:"Arti & Bacaan",meaning_usage:"Arti & Penggunaan",function_context:"Fungsi & Konteks"};
function weakness(reviews:ReviewRow[]){const byItem=new Map<string,ReviewRow[]>(),byAspect=new Map<string,ReviewRow[]>();for(const r of reviews){const ik=`${r.item_type}:${r.item_id}`,ia=byItem.get(ik)??[];ia.push(r);byItem.set(ik,ia);const ak=r.aspect||r.direction||r.item_type,aa=byAspect.get(ak)??[];aa.push(r);byAspect.set(ak,aa)}const item=new Map<string,number>();for(const[k,a0]of byItem){const a=a0.slice(0,8),n=Math.max(1,a.length),wrong=a.filter(x=>x.rating<2).length/n,hint=a.filter(x=>x.used_hint).length/n,slow=a.filter(x=>Number(x.response_ms??0)>8000).length/n,dirs=new Set(a.map(x=>x.direction).filter(Boolean)).size;item.set(k,wrong*.55+hint*.18+slow*.15+(dirs<2?.12:0))}const aspects=[...byAspect].map(([key,a])=>{const n=Math.max(1,a.length),good=a.filter(x=>x.rating>=2).length/n,hint=a.filter(x=>x.used_hint).length/n,slow=a.filter(x=>Number(x.response_ms??0)>8000).length/n;return{key,label:aspectLabel[key]||key,score:(1-good)*.65+hint*.2+slow*.15,total:a.length}}).sort((a,b)=>b.score-a.score);return{item,aspects,weakest:aspects[0]??null}}
async function itemSuggestion(client:any,r:{item_type:string;item_id:string},weak=false,aspect?:string):Promise<AdaptiveSuggestion|null>{const prefix=weak?`Prioritas ${aspectLabel[aspect||'']||aspect||'kelemahan'}`:"Review";if(r.item_type==="kanji"){const{data}=await client.from("kanji").select("character,meaning_id").eq("id",r.item_id).maybeSingle();return data?{id:r.item_id,label:String(data.character??"Kanji"),subtitle:`${prefix} · ${data.meaning_id??"Kanji"}`}:null}if(r.item_type==="vocabulary"){const{data}=await client.from("vocabulary").select("term,meaning_id").eq("id",r.item_id).maybeSingle();return data?{id:r.item_id,label:String(data.term??"Kosakata"),subtitle:`${prefix} · ${data.meaning_id??"Kosakata"}`}:null}if(r.item_type==="grammar"){const{data}=await client.from("grammar_points").select("pattern,meaning_id").eq("id",r.item_id).maybeSingle();return data?{id:r.item_id,label:String(data.pattern??"Bunpou"),subtitle:`${prefix} · ${data.meaning_id??"Bunpou"}`}:null}return null}
async function enrichTasksWithSuggestions(userId:string,level:string,tasks:AdaptiveTask[]):Promise<AdaptiveTask[]>{const client=supabase as any;const[{data:progress},{data:reviewData}]=await Promise.all([client.from("user_item_progress").select("item_type,item_id,status,due_at,ease_factor").eq("user_id",userId).eq("level",level),client.from("flashcard_reviews").select("item_type,item_id,rating,used_hint,response_ms,direction,aspect,created_at").eq("user_id",userId).eq("level",level).order("created_at",{ascending:false}).limit(500)]);const rows=(progress??[]) as ProgressRow[],reviews=(reviewData??[]) as ReviewRow[],w=weakness(reviews),nowIso=new Date().toISOString();const mastered=(type:string)=>new Set(rows.filter(r=>r.item_type===type&&r.status==="mastered").map(r=>r.item_id));const known=(type:string)=>new Set(rows.filter(r=>r.item_type===type).map(r=>r.item_id));const aspectItems=new Map<string,number>();for(const r of reviews){if(!r.aspect)continue;const k=`${r.item_type}:${r.item_id}`,penalty=(r.rating<2?.65:0)+(r.used_hint?.2:0)+(Number(r.response_ms??0)>8000?.15:0);if(r.aspect===w.weakest?.key)aspectItems.set(k,(aspectItems.get(k)??0)+penalty)}return Promise.all(tasks.map(async task=>{const wanted=Math.max(1,Math.min(12,Number(task.target_count||1)));try{if(task.task_type==="review"){const due=rows.filter(r=>r.due_at&&r.due_at<=nowIso).map(r=>({...r,score:3+(w.item.get(`${r.item_type}:${r.item_id}`)??0)+(aspectItems.get(`${r.item_type}:${r.item_id}`)??0)}));const weakNotDue=rows.filter(r=>!(r.due_at&&r.due_at<=nowIso)).map(r=>({...r,score:(w.item.get(`${r.item_type}:${r.item_id}`)??0)+(aspectItems.get(`${r.item_type}:${r.item_id}`)??0)})).filter(r=>r.score>=.28);const queue=[...due.sort((a,b)=>b.score-a.score),...weakNotDue.sort((a,b)=>b.score-a.score)].slice(0,wanted);const suggestions=(await Promise.all(queue.map(r=>itemSuggestion(client,r,r.score<3,w.weakest?.key)))).filter(Boolean) as AdaptiveSuggestion[];return{...task,priority:queue.length?120:task.priority,target_count:Math.max(task.target_count,Math.min(12,queue.length)),reason:queue.length?`Review jatuh tempo diprioritaskan, lalu kelemahan ${w.weakest?.label??"recall"}, sebelum materi baru.`:task.reason,suggestions,metadata:{...(task.metadata??{}),smartReview:true,weakCount:weakNotDue.length,dueCount:due.length,weakestAspect:w.weakest?.key??null,weakestAspectLabel:w.weakest?.label??null,weakestAspectScore:w.weakest?Math.round(w.weakest.score*100):null}}}const newPenalty=w.weakest&&w.weakest.score>=.35?25:0;const adjusted={...task,priority:Math.max(1,task.priority-newPenalty),metadata:{...(task.metadata??{}),weaknessGuard:newPenalty>0,weakestAspect:w.weakest?.key??null}};if(task.task_type==="new_kanji"){const skip=known("kanji");const{data}=await client.from("kanji").select("id,character,meaning_id").eq("level",level).eq("is_published",true).order("sort_order",{ascending:true}).limit(wanted*4);return{...adjusted,suggestions:(data??[]).filter((x:any)=>!skip.has(String(x.id))).slice(0,wanted).map((x:any)=>({id:String(x.id),label:String(x.character??"Kanji"),subtitle:x.meaning_id?String(x.meaning_id):null}))}}if(task.task_type==="new_vocabulary"){const skip=known("vocabulary");const{data}=await client.from("vocabulary").select("id,term,reading,meaning_id").eq("level",level).eq("is_published",true).order("sort_order",{ascending:true}).limit(wanted*4);return{...adjusted,suggestions:(data??[]).filter((x:any)=>!skip.has(String(x.id))).slice(0,wanted).map((x:any)=>({id:String(x.id),label:String(x.term??"Kosakata"),subtitle:[x.reading,x.meaning_id].filter(Boolean).map(String).join(" · ")}))}}if(task.task_type==="new_grammar"){const skip=known("grammar");const{data}=await client.from("grammar_points").select("id,pattern,meaning_id").eq("level",level).eq("is_published",true).order("sort_order",{ascending:true}).limit(wanted*4);return{...adjusted,suggestions:(data??[]).filter((x:any)=>!skip.has(String(x.id))).slice(0,wanted).map((x:any)=>({id:String(x.id),label:String(x.pattern??"Bunpou"),subtitle:x.meaning_id?String(x.meaning_id):null}))}}if(task.task_type==="reading"){const skip=mastered("reading");const{data}=await client.from("reading_passages").select("id,title").eq("level",level).eq("is_published",true).order("sort_order",{ascending:true}).limit(wanted*3);return{...adjusted,suggestions:(data??[]).filter((x:any)=>!skip.has(String(x.id))).slice(0,wanted).map((x:any)=>({id:String(x.id),label:String(x.title??"Dokkai")}))}}if(task.task_type==="listening"){const skip=mastered("listening");const{data}=await client.from("listening_items").select("id,title,duration_seconds").eq("level",level).eq("is_published",true).order("sort_order",{ascending:true}).limit(wanted*3);return{...adjusted,suggestions:(data??[]).filter((x:any)=>!skip.has(String(x.id))).slice(0,wanted).map((x:any)=>({id:String(x.id),label:String(x.title??"Choukai"),subtitle:x.duration_seconds?`${Math.ceil(Number(x.duration_seconds)/60)} menit`:null}))}}return adjusted}catch{return task}}))}
export async function fetchAdaptivePlan():Promise<AdaptivePlan>{const{data:auth,error:authError}=await supabase.auth.getUser();if(authError||!auth.user)return emptyPlan;const client=supabase as any;await client.rpc("ensure_active_study_plan",{});await client.rpc("generate_daily_study_tasks",{});await client.rpc("sync_daily_study_task_progress",{});const today=new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Tokyo",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date());const[{data:plans},{data:tasks}]=await Promise.all([client.from("study_plans").select("id,target_level,target_date,status").eq("user_id",auth.user.id).eq("status","active").order("created_at",{ascending:false}).limit(1),client.from("daily_study_tasks").select("id,task_type,target_count,completed_count,priority,reason,metadata").eq("user_id",auth.user.id).eq("study_date",today).order("priority",{ascending:false})]);const plan=plans?.[0];if(!plan)return emptyPlan;const taskRows=[...(await enrichTasksWithSuggestions(auth.user.id,String(plan.target_level??"N5"),(tasks??[]) as AdaptiveTask[]))].sort((a,b)=>b.priority-a.priority);const target=taskRows.reduce((s,t)=>s+Number(t.target_count||0),0),completed=taskRows.reduce((s,t)=>s+Math.min(Number(t.completed_count||0),Number(t.target_count||0)),0),targetMs=new Date(`${plan.target_date}T00:00:00+09:00`).getTime(),todayMs=new Date(`${today}T00:00:00+09:00`).getTime(),daysLeft=Math.max(0,Math.ceil((targetMs-todayMs)/86400000));return{active:true,targetLevel:plan.target_level??null,targetDate:plan.target_date??null,daysLeft,tasks:taskRows,target,completed}}
export const adaptiveTaskLabels:Record<AdaptiveTaskType,string>={new_kanji:"Kanji baru",new_vocabulary:"Kotoba baru",new_grammar:"Bunpō baru",review:"Review",quiz:"Kuis",reading:"Dokkai",listening:"Listening"};

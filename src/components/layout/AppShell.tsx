import { Link, useRouterState } from "@tanstack/react-router";
import { ArrowLeft,Bell,BookOpen,GraduationCap,Home,ListChecks,Moon,Settings,Sun,Target,Trophy } from "lucide-react";
import { useEffect,useState,type ReactNode } from "react";
import { Button } from "@/components/ui/button";
import { cn } from "@/lib/utils";
import { BrandMark } from "@/components/layout/BrandMark";
import { JapaneseSpacing } from "@/components/japanese/JapaneseSpacing";
import { supabase } from "@/integrations/supabase/client";
const navItems=[{to:"/target",label:"Target",icon:Target},{to:"/belajar",label:"Materi",icon:BookOpen},{to:"/dashboard",label:"Home",icon:Home},{to:"/simulasi",label:"Simulasi",icon:ListChecks},{to:"/kelas",label:"Kelas",icon:GraduationCap}] as const;
type Props={title:string;description?:string;backTo?:string;backLabel?:string;compact?:boolean;children:ReactNode};
export function AppShell({title,description,backTo,backLabel="Kembali",compact=false,children}:Props){const pathname=useRouterState({select:s=>s.location.pathname});const[darkMode,setDarkMode]=useState(false);const[unreadNotifications,setUnreadNotifications]=useState(0);useEffect(()=>{const saved=window.localStorage.getItem("enonihongo-theme")==="dark";setDarkMode(saved);document.documentElement.classList.toggle("dark",saved)},[]);useEffect(()=>{let active=true;async function loadUnread(){const{data:auth}=await supabase.auth.getUser();if(!auth.user){if(active)setUnreadNotifications(0);return}const{count}=await supabase.from("user_notifications" as never).select("id",{count:"exact",head:true}).eq("user_id",auth.user.id).is("read_at",null);if(active)setUnreadNotifications(count??0)}void loadUnread();return()=>{active=false}},[pathname]);function toggleTheme(){setDarkMode(current=>{const next=!current;document.documentElement.classList.toggle("dark",next);window.localStorage.setItem("enonihongo-theme",next?"dark":"light");return next})}const isActive=(to:string)=>pathname===to||pathname.startsWith(`${to}/`);return <div className="relative min-h-screen bg-background pb-[calc(5.25rem+env(safe-area-inset-bottom))] text-foreground md:pb-8"><header className="sticky top-0 z-30 border-b border-border/55 bg-background/95 backdrop-blur-xl"><div className="mx-auto flex h-14 w-full max-w-5xl items-center justify-between px-4">{backTo?<Link to={backTo} aria-label={backLabel} className="flex min-w-0 items-center gap-2"><ArrowLeft className="size-[18px] shrink-0"/><span className="truncate text-[14px] font-semibold">{title}</span></Link>:<Link to="/dashboard" className="flex items-center gap-2" aria-label="ENO NIHONGO"><BrandMark/></Link>}<div className="flex items-center gap-0.5">{!backTo&&<><Link to="/notifikasi" className="relative grid size-9 place-items-center rounded-xl text-muted-foreground" aria-label="Pemberitahuan"><Bell className="size-[17px]"/>{unreadNotifications>0&&<span className="absolute right-1.5 top-1.5 grid min-w-3.5 place-items-center rounded-full bg-destructive px-1 text-[8px] font-bold leading-3.5 text-destructive-foreground">{unreadNotifications>9?"9+":unreadNotifications}</span>}</Link><Link to="/leaderboard" className={cn("grid size-9 place-items-center rounded-xl",isActive('/leaderboard')?"bg-primary/10 text-primary":"text-muted-foreground")} aria-label="Leaderboard" title="Leaderboard"><Trophy className="size-[17px]"/></Link></>}<Button variant="ghost" size="icon" className="size-9 rounded-xl" onClick={toggleTheme}>{darkMode?<Sun className="size-[17px]"/>:<Moon className="size-[17px]"/>}</Button>{!backTo&&<Link to="/pengaturan" className="grid size-9 place-items-center rounded-xl text-muted-foreground"><Settings className="size-[17px]"/></Link>}</div></div><nav className="mx-auto hidden max-w-5xl px-4 pb-2 md:block"><ul className="flex items-center gap-1">{navItems.map(item=><li key={item.to}><Link to={item.to} className={cn("flex items-center gap-1.5 rounded-lg px-3 py-1.5 text-xs font-medium",isActive(item.to)?"bg-primary/10 text-primary":"text-muted-foreground hover:bg-muted/50")}><item.icon className="size-3.5"/>{item.label}</Link></li>)}</ul></nav></header><main className={cn("relative z-10 mx-auto w-full max-w-5xl px-4 py-5",compact&&"py-4")}><JapaneseSpacing>{!compact&&description&&<p className="mb-4 text-[12px] leading-5 text-muted-foreground">{description}</p>}{children}</JapaneseSpacing></main><nav className="fixed inset-x-0 bottom-0 z-40 border-t border-border/70 bg-background/95 pb-[env(safe-area-inset-bottom)] backdrop-blur-xl md:hidden"><ul className="mx-auto flex h-[4.15rem] w-full max-w-lg items-center px-1">{navItems.map(item=>{const active=isActive(item.to);const Icon=item.icon;return <li key={item.to} className="h-full flex-1"><Link to={item.to} className="group flex h-full w-full items-center justify-center"><span className={cn("flex min-w-[3.2rem] flex-col items-center justify-center gap-1 rounded-2xl px-1 py-1.5",active?"text-primary":"text-muted-foreground")}><span className={cn("grid size-8 place-items-center rounded-xl",active&&"bg-primary/10")}><Icon className="size-[19px]"/></span><span className="text-[9.5px] font-medium">{item.label}</span></span></Link></li>})}</ul></nav></div>}
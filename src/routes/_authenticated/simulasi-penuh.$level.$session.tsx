import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { useQuery } from "@tanstack/react-query";
import { useCallback, useEffect, useMemo, useRef, useState } from "react";
import { ArrowLeft, ArrowRight, BookOpenText, Check, Clock3, Headphones, Play, RotateCcw } from "lucide-react";
import { AppShell } from "@/components/layout/AppShell";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { supabase } from "@/integrations/supabase/client";
import { jlptSessions, sectionLabels, type SimulationSection } from "@/lib/jlpt-simulation-config";
import type { FullProgress } from "@/lib/jlpt-simulation-result";
import type { Level } from "@/lib/learn-queries";
export const Route=createFileRoute("/_authenticated/simulasi-penuh/$level/$session")({component:FullSessionRunner});
type Row={id:string;section:SimulationSection;mondai_no:number;question_no:number;question_type:string;instruction_jp:string;prompt_jp:string;choices:string[];passage_title:string|null;passage_jp:string|null;audio_url:string|null;transcript_jp:string|null};type Result={total_questions:number;correct_count:number;score_percent:number};
const fmt=(s:number)=>`${String(Math.floor(s/60)).padStart(2,"0")}:${String(s%60).padStart(2,"0")}`;
async function fetchQuestions(level:Level,sections:SimulationSection[]){const{data,error}=await(supabase as any).from("jlpt_simulation_questions_public").select("id,level,section,mondai_no,question_no,question_type,instruction_jp,prompt_jp,choices,passage_title,passage_jp,audio_url,transcript_jp").eq("level",level).in("section",sections);if(error)throw error;const order=new Map(sections.map((s,i)=>[s,i]));return(data??[]).filter((x:any)=>x.prompt_jp&&Array.isArray(x.choices)&&x.choices.length===4).sort((a:any,b:any)=>(order.get(a.section)??99)-(order.get(b.section)??99)||a.mondai_no-b.mondai_no||a.question_no-b.question_no) as Row[]}
function Audio({url}:{url:string|null}){const ref=useRef<HTMLAudioElement|null>(null);if(!url)return <div className="rounded-xl border border-dashed p-4 text-center"><Headphones className="mx-auto mb-2 size-5 text-muted-foreground"/><p className="text-xs font-medium">音声問題</p><p className="mt-1 text-[10px] text-muted-foreground">専用音声を準備中です。</p></div>;return <div className="flex gap-2"><audio ref={ref} src={url}/><Button size="sm" onClick={()=>void ref.current?.play()}><Play className="mr-1 size-4"/>再生</Button><Button size="sm" variant="outline" onClick={()=>{if(ref.current){ref.current.currentTime=0;void ref.current.play()}}}><RotateCcw className="mr-1 size-4"/>最初から</Button></div>}
function FullSessionRunner(){const{level:raw,session:rawSession}=Route.useParams();const navigate=useNavigate();const level=raw.toUpperCase() as Level;const sessionIndex=Math.max(0,Number(rawSession));const sessions=jlptSessions[level]??[];const session=sessions[sessionIndex];const storageKey=`eno-jlpt-full-${level}`;const serverKey=`${storageKey}-server-id`;const answerKey=`${storageKey}-session-${sessionIndex}-answers`;const deadlineKey=`${storageKey}-session-${sessionIndex}-deadline`;const sections=session?.sections??[];const q=useQuery({queryKey:["simulation-full-session",level,sessionIndex,sections.join("-")],queryFn:()=>fetchQuestions(level,sections),enabled:!!session});const questions=q.data??[];const[index,setIndex]=useState(0);const[answers,setAnswers]=useState<Record<string,number>>({});const[remaining,setRemaining]=useState((session?.minutes??0)*60);const[saving,setSaving]=useState(false);const[error,setError]=useState<string|null>(null);const finishing=useRef(false);const answersRef=useRef<Record<string,number>>({});const current=questions[index];
useEffect(()=>{try{const a=window.localStorage.getItem(answerKey);if(a)setAnswers(JSON.parse(a));const existing=Number(window.localStorage.getItem(deadlineKey));if(!existing&&session)window.localStorage.setItem(deadlineKey,String(Date.now()+session.minutes*60000))}catch{}},[answerKey,deadlineKey,session]);useEffect(()=>{answersRef.current=answers;try{window.localStorage.setItem(answerKey,JSON.stringify(answers))}catch{}},[answers,answerKey]);
const ensureServerSession=useCallback(async()=>{const existing=window.localStorage.getItem(serverKey);if(existing)return existing;const{data,error:e}=await(supabase as any).rpc("start_jlpt_simulation_full",{p_level:level});if(e)throw e;const id=String(data);window.localStorage.setItem(serverKey,id);return id},[serverKey,level]);
const finish=useCallback(async()=>{if(finishing.current||!session)return;finishing.current=true;setSaving(true);setError(null);try{const fullId=await ensureServerSession();const results:Record<string,Result>={};const deadline=Number(window.localStorage.getItem(deadlineKey));const elapsed=Math.max(0,Math.min(session.minutes*60,session.minutes*60-Math.max(0,Math.ceil((deadline-Date.now())/1000))));for(const section of session.sections){const sectionAnswers=questions.filter(x=>x.section===section).map(x=>({question_id:x.id,selected_index:answersRef.current[x.id]})).filter(x=>x.selected_index!==undefined);const{data,error:e}=await(supabase as any).rpc("submit_jlpt_simulation_full_section",{p_full_session_id:fullId,p_session_index:sessionIndex,p_level:level,p_section:section,p_duration_seconds:elapsed,p_answers:sectionAnswers});if(e)throw e;const r=Array.isArray(data)?data[0]:data;if(!r)throw new Error("Hasil sesi tidak tersedia");results[`${sessionIndex}:${section}`]={total_questions:Number(r.total_questions),correct_count:Number(r.correct_count),score_percent:Number(r.score_percent)}}const rawProgress=window.localStorage.getItem(storageKey);const p:FullProgress=rawProgress?JSON.parse(rawProgress):{sessionIndex,sectionIndex:0,startedAt:Date.now(),completed:[],results:{}};const completed=Array.from(new Set([...(p.completed??[]),...session.sections.map(s=>`${sessionIndex}:${s}`)]));const nextIndex=sessionIndex+1;const next:FullProgress={...p,sessionIndex:nextIndex,sectionIndex:0,completed,results:{...(p.results??{}),...results}};window.localStorage.setItem(storageKey,JSON.stringify(next));window.localStorage.removeItem(answerKey);window.localStorage.removeItem(deadlineKey);if(nextIndex<sessions.length){void navigate({to:"/simulasi-penuh/$level/$session",params:{level,session:String(nextIndex)}})}else{const{data:finalData,error:finalError}=await(supabase as any).rpc("finalize_jlpt_simulation_full",{p_full_session_id:fullId});if(finalError)throw finalError;const finalRow=Array.isArray(finalData)?finalData[0]:finalData;if(finalRow){window.localStorage.setItem(`${storageKey}-server-result`,JSON.stringify(finalRow));next.completedAt=new Date(finalRow.completed_at).getTime();window.localStorage.setItem(storageKey,JSON.stringify(next))}void navigate({to:"/simulasi-penuh/$level",params:{level}})}}catch(e){console.error(e);setError("Sesi belum berhasil dikirim. Jawaban tetap tersimpan; silakan coba lagi.");finishing.current=false}finally{setSaving(false)}},[session,questions,level,sessionIndex,storageKey,serverKey,answerKey,deadlineKey,sessions.length,navigate,ensureServerSession]);
useEffect(()=>{if(!session||saving)return;const tick=()=>{const deadline=Number(window.localStorage.getItem(deadlineKey))||Date.now()+session.minutes*60000;const left=Math.max(0,Math.ceil((deadline-Date.now())/1000));setRemaining(left);if(left===0)void finish()};tick();const id=window.setInterval(tick,1000);return()=>window.clearInterval(id)},[session,deadlineKey,finish,saving]);
const sectionNav=useMemo(()=>sections.map(s=>({section:s,first:questions.findIndex(q=>q.section===s),count:questions.filter(q=>q.section===s).length,done:questions.filter(q=>q.section===s&&answers[q.id]!==undefined).length})).filter(x=>x.first>=0),[sections,questions,answers]);if(!session)return <AppShell title="Simulasi Penuh"><p className="py-10 text-center text-sm">Sesi tidak ditemukan.</p></AppShell>;if(q.isLoading)return <AppShell title={`JLPT ${level}`}><p className="py-10 text-center text-xs text-muted-foreground">問題を読み込んでいます…</p></AppShell>;if(!current)return <AppShell title={`JLPT ${level}`}><p className="py-10 text-center text-sm">Bank soal sesi ini belum tersedia.</p></AppShell>;const star=current.question_type==="sentence_composition";return <AppShell title={`JLPT ${level} · 第${sessionIndex+1}セッション`} compact><div className="mx-auto max-w-2xl space-y-3"><div className="sticky top-0 z-20 rounded-xl border bg-background/95 p-2 backdrop-blur"><div className="flex items-center justify-between"><span className="text-[10px] font-semibold">{session.labelJp}</span><span className={`inline-flex items-center gap-1 rounded-full px-2 py-1 text-[11px] font-bold ${remaining<=300?"bg-destructive/10 text-destructive":"bg-primary/10 text-primary"}`}><Clock3 className="size-3"/>{fmt(remaining)}</span></div><div className="mt-2 flex gap-1 overflow-x-auto">{sectionNav.map(x=><button key={x.section} onClick={()=>setIndex(x.first)} className={`shrink-0 rounded-lg border px-3 py-1.5 text-[10px] font-semibold ${current.section===x.section?"border-primary bg-primary text-primary-foreground":""}`}>{sectionLabels[x.section]} {x.done}/{x.count}</button>)}</div></div><Card><CardContent className="p-4"><p className="text-[10px] font-semibold text-muted-foreground">{sectionLabels[current.section]} · 問題 {current.mondai_no} · 問 {current.question_no}</p><p className="mt-2 font-jp text-xs leading-6">{current.instruction_jp}</p></CardContent></Card>{current.section==="reading"&&current.passage_jp&&<Card><CardContent className="max-h-[44vh] overflow-y-auto p-4"><div className="mb-2 flex items-center gap-2 text-primary"><BookOpenText className="size-4"/><span className="text-xs font-semibold">{current.passage_title||"文章"}</span></div><p className="whitespace-pre-wrap font-jp text-sm leading-7">{current.passage_jp}</p></CardContent></Card>}{current.section==="listening"&&<Card><CardContent className="p-4"><Audio url={current.audio_url}/></CardContent></Card>}<Card><CardContent className="p-4">{star&&<p className="mb-3 text-[11px] text-muted-foreground">文全体が正しくなるように並べたとき、★に入るものを選んでください。</p>}<h1 className={`font-jp font-semibold leading-8 ${star?"rounded-xl bg-muted/40 p-4 text-center text-base":"text-[15px]"}`}>{current.prompt_jp}</h1><div className={star?"mt-4 grid grid-cols-2 gap-2":"mt-4 space-y-2"}>{current.choices.map((choice,i)=><button key={i} onClick={()=>setAnswers(v=>({...v,[current.id]:i}))} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left font-jp text-xs ${answers[current.id]===i?"border-primary bg-primary/5 ring-1 ring-primary/30":""}`}><span className="grid size-6 shrink-0 place-items-center rounded-full border font-bold">{i+1}</span>{choice}</button>)}</div></CardContent></Card>{error&&<p className="rounded-lg bg-destructive/10 p-2 text-xs text-destructive">{error}</p>}<div className="flex justify-between gap-2"><Button variant="outline" disabled={index===0||saving} onClick={()=>setIndex(i=>i-1)}><ArrowLeft className="mr-1 size-4"/>前へ</Button>{index===questions.length-1?<Button disabled={saving} onClick={()=>void finish()}><Check className="mr-1 size-4"/>{saving?"送信中…":"セッション終了"}</Button>:<Button disabled={saving} onClick={()=>setIndex(i=>i+1)}>次へ<ArrowRight className="ml-1 size-4"/></Button>}</div></div></AppShell>}

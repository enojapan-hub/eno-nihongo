import { createFileRoute } from "@tanstack/react-router";
import { useQuery,useQueryClient } from "@tanstack/react-query";
import { CheckCircle2,Image,Users,XCircle } from "lucide-react";
import { AppShell } from "@/components/layout/AppShell";
import { Card,CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { supabase } from "@/integrations/supabase/client";
import { useState } from "react";
export const Route=createFileRoute("/_authenticated/admin-kelas")({component:Page});
function Page(){const qc=useQueryClient();const[filter,setFilter]=useState('review');const[msg,setMsg]=useState('');const gate=useQuery({queryKey:['admin-class-gate'],queryFn:async()=>{const{data,error}=await(supabase as any).rpc('get_admin_overview');if(error)throw error;return data}});const q=useQuery({queryKey:["admin-classes"],enabled:!!gate.data,queryFn:async()=>{const{data,error}=await(supabase as any).from("classes").select("*").order("created_at",{ascending:false});if(error)throw error;return data??[]}});async function status(id:string,value:string){setMsg('');const{error}=await(supabase as any).from("classes").update({status:value,updated_at:new Date().toISOString()}).eq("id",id);if(error){setMsg(error.message);return}setMsg(value==='published'?'Kelas dipublikasikan.':'Kelas ditolak.');void qc.invalidateQueries({queryKey:["admin-classes"]})}if(gate.isLoading)return <AppShell title="Kelas" backTo="/admin"><p className="p-4 text-xs">Memeriksa akses…</p></AppShell>;if(gate.isError)return <AppShell title="Kelas" backTo="/admin"><p className="p-4 text-xs text-destructive">Akses Admin diperlukan.</p></AppShell>;const all=q.data??[];const shown=filter==='all'?all:all.filter((x:any)=>x.status===filter);const count=(s:string)=>all.filter((x:any)=>x.status===s).length;return <AppShell title="Kelas" backTo="/admin"><div className="space-y-4"><div><h1 className="text-xl font-black">Kelola Kelas</h1><p className="text-xs text-muted-foreground">Review kelas guru, banner, status publikasi dan peserta.</p></div><div className="grid gap-3 sm:grid-cols-2"><Card><CardContent className="p-4"><Image className="mb-2 size-5 text-primary"/><p className="font-bold">Banner Halaman Kelas</p><p className="text-[10px] text-muted-foreground">Banner utama, CTA, periode tayang dan urutan.</p></CardContent></Card><Card><CardContent className="p-4"><Users className="mb-2 size-5 text-primary"/><p className="font-bold">Kelas Guru</p><p className="text-[10px] text-muted-foreground">{all.length} kelas · {count('review')} menunggu review.</p></CardContent></Card></div><div className="flex gap-2 overflow-x-auto">{[['review','Review'],['published','Published'],['draft','Draft'],['rejected','Ditolak'],['all','Semua']].map(([id,label])=><Button key={id} size="sm" variant={filter===id?'default':'outline'} onClick={()=>setFilter(id)}>{label}{id!=='all'?` (${count(id)})`:''}</Button>)}</div>{msg&&<p className="rounded-xl bg-muted p-3 text-xs">{msg}</p>}{q.isError&&<p className="text-xs text-destructive">Daftar kelas gagal dimuat.</p>}<div className="space-y-2">{shown.map((c:any)=><Card key={c.id}><CardContent className="flex items-center gap-3 p-3">{c.banner_url&&<img src={c.banner_url} className="h-14 w-24 rounded-xl object-cover" alt=""/>}<div className="min-w-0 flex-1"><p className="truncate text-xs font-bold">{c.title}</p><p className="text-[10px] text-muted-foreground">{c.level} · {c.status} · {Number(c.price)>0?`${c.currency} ${c.price}`:'Gratis'}</p></div>{c.status!=="published"&&<Button size="sm" onClick={()=>status(c.id,"published")}><CheckCircle2 className="mr-1 size-3"/>Publish</Button>}{c.status!=="rejected"&&<Button size="sm" variant="outline" onClick={()=>status(c.id,"rejected")}><XCircle className="size-3"/></Button>}</CardContent></Card>)}{!q.isLoading&&shown.length===0&&<p className="rounded-xl bg-muted p-4 text-xs text-muted-foreground">Tidak ada kelas pada status ini.</p>}</div></div></AppShell>}
